Next.js project with better-auth

## Getting Started

Commands:

```bash
# to install db (first thing)
yarn db:install
# to build
yarn build
# run in dev mode
yarn dev
# run in prodution mode
yarn start
```